services:
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
  postgres:
    image: 'postgres:16'
    environment:
      POSTGRES_DB: flowmat
      POSTGRES_USER: flowmat
      POSTGRES_PASSWORD: flowmat
    ports:
      - '5432:5432'
